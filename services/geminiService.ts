// AI Feature has been disabled/removed.
// This file is kept as a placeholder to prevent import errors if referenced elsewhere, 
// but the logic relying on process.env and @google/genai has been removed.

import { PunchRecord } from "../types";

export const analyzeAttendance = async (records: PunchRecord[], monthName: string): Promise<string> => {
  return "Analisis AI telah dinyahaktifkan.";
};